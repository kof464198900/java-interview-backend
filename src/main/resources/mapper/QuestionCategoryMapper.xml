<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.interview.mapper.QuestionCategoryMapper">
    
    <!-- 查询分类及其题目数量 -->
    <select id="selectCategoryWithCount" resultType="com.interview.vo.CategoryVO">
        SELECT c.id, c.name, c.icon, c.sort, COUNT(q.id) as questionCount
        FROM question_category c
        LEFT JOIN question q ON c.id = q.category_id AND q.status = 1
        GROUP BY c.id
        ORDER BY c.sort
    </select>
    
</mapper>
